export default function LoadingData() {
  return (
    <div className='flex w-full h-full items-center justify-center gap-2 bg-gray'>
      <p>Wait a little, data is loading...</p>
      <p>(^-^ &quot;)</p>
    </div>
  );
}
